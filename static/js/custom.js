document.addEventListener('DOMContentLoaded', function () {    const titleInput = document.querySelector('input[name="title"]');    const authorInput = document.querySelector('input[name="author"]');    const designSelect = document.querySelector('select[name="design"]');    const bookTitle = document.getElementById('bookTitle');    const bookAuthor = document.getElementById('bookAuthor');    const coverImage = document.getElementById('coverImage');    const textOverlay = document.getElementById('textOverlay');    // При вводе текста    titleInput.addEventListener('input', () => {        bookTitle.textContent = titleInput.value || 'Название книги';    });    authorInput.addEventListener('input', () => {        bookAuthor.textContent = authorInput.value || 'Имя автора';    });    // При выборе дизайна    designSelect.addEventListener('change', () => {        const selected = designSelect.value;        if (!selected) {            coverImage.style.display = 'none';            textOverlay.style.display = 'none';            return;        }        coverImage.src = `/static/covers/${selected}.jpg`;        coverImage.style.display = 'block';        textOverlay.style.display = 'block';    });});