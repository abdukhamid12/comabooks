// JavaScriptconst typedWordElement = document.getElementById('typed-word');const words = [  "родителям",  "папе",  "маме",  "мужу",  "жене",  "брату",  "сестре",  "друзьям",  "коллеге"];const typingDelay = 70;const removingDelay = 70;const newWordDelay = 1000;let wordIndex = 0;let charIndex = 0;let isRemoving = false;function animateWord() {  const currentWord = words[wordIndex];  if (!isRemoving && charIndex <= currentWord.length) {    // Печатаем по одной букве    typedWordElement.textContent = currentWord.substring(0, charIndex);    charIndex++;    setTimeout(animateWord, typingDelay);  } else if (isRemoving && charIndex >= 0) {    // Удаляем по одной букве    typedWordElement.textContent = currentWord.substring(0, charIndex);    charIndex--;    setTimeout(animateWord, removingDelay);  } else {    // Переключение между печатью и удалением    isRemoving = !isRemoving;    if (!isRemoving) {      wordIndex = (wordIndex + 1) % words.length;    }    setTimeout(animateWord, newWordDelay);  }}document.addEventListener('DOMContentLoaded', () => {  animateWord();});